import { ViewStyle } from 'react-native';

export const cardShadow: ViewStyle = {
  shadowColor: '#141416',
  shadowOpacity: 0.05,
  shadowRadius: 14,
  shadowOffset: { width: 0, height: 8 },
  elevation: 3,
};

export const dockShadow: ViewStyle = {
  shadowColor: '#141416',
  shadowOpacity: 0.14,
  shadowRadius: 16,
  shadowOffset: { width: 0, height: 10 },
  elevation: 8,
};
