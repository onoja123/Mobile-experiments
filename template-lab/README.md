# Template Lab

Photo template browser. Swipe through big template cards — camera-back LCD, Pantone swatch, viewfinder grid, full-bleed shots — with parallax as you go, then drop your own photos in.

## Run

```
npm install
npx expo start
```

Expo SDK 57, NativeWind, Reanimated. Poppins via expo-google-fonts.
