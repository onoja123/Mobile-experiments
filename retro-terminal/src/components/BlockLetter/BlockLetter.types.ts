export type BlockLetterBar = [left: number, top: number, width: number, height: number];

export type BlockLetterProps = {
  char: string;
  width: number;
  height: number;
  thickness: number;
  color?: string;
};
