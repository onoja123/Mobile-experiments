# neobank-wallet

Neobank wallet screen — card stack that expands/collapses with a spring animation, recent transactions below.

Expo 57, expo-router, Reanimated, Gesture Handler, SVG, NativeWind.

## Run

```
npm install
npx expo start
```
