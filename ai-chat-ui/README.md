# ai-chat-ui

Chat UI experiment — drawer sidebar with chat history, message thread with code blocks, and a send button that morphs into the outgoing message.

Expo 57, expo-router, Reanimated, NativeWind.

## Run

```
npm install
npx expo start
```
