# reel-rate

![reel-rate demo](demo.gif)

Tap "Rate this movie" and the page pushes up while a dark panel grows from the bottom. The stars slide in one after another, picking one pops the score and its label, and the close button morphs into Save.

```
npm install
npm start
```

Open in Expo Go.
